apiVersion: v1
kind: Service
metadata:
  name: {{ include "kafka-debug-flow.fullname" . }}
  labels:
    {{- include "kafka-debug-flow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "kafka-debug-flow.selectorLabels" . | nindent 4 }}
